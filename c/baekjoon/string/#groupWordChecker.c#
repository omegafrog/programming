#include<stdio.h>
#include<malloc.h>
#include<string.h>

int main(){
  int wordNum;
  scanf("%d",&wordNum);

  char **p;
  p=(char**)malloc(sizeof(char*)*wordNum);
  *p=(char*)malloc(sizeof(char)*105);

  for(int i=0;i<wordNum;i++){
    fgets(*(p+i),100,stdin);
    *(*(p+i)+strlen(*(p+i)-1))=0;
    printf("%c",*(*(p+i)+strlen(*(p+i)-1))=0);
  }

  int alpabet[100];
  char temp;
  int i=0;
  for(int x=0;x<wordNum;x++){
    while(*(*(p+x)+i)!=0){
      
      
  