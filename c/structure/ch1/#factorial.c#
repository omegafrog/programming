#include<stdio.h>

int factorial(int);

int main(){
  int n;
  scanf("%d",&n);

  int res=0;
  res = factorial(n);

  printf("%d",res);
}

int factorial(int n){
    if(n<=1){
    return(1);
  }
  else return (n*factorial(n-1));
}

  
