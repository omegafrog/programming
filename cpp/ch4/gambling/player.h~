#ifndef __PLAYER_H__
#define __PLAYER_H__

#include <iostream>
#include <cstdlib>
#include <ctime>
using namespace std;


class player{
 private:
  string name;
  int slots[3]; // array for random num
 protected:
  
 public:
  player();
  ~player();


  void setSlots();
  string getName();
  int* getSlots();
};
#endif
