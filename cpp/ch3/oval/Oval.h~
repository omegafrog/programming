#ifndef __OVAL_H__
#define __OVAL_H__

class Oval{
  int width;
  int height;

 public:
  Oval();
  Oval(int width, int height);

  double getWidth();
  double getHeight();
  void set(int w, int h);
  void show();
};

