#ifndef __PALINDROME_H__
#define __PALINDROME_H__

#define MAX_STRING_LEN 100

// 회문을 판별하는 함수
int is_palindrome(char term[]);
// 문자열의 문자가 알파벳인지 판별하는 함수.
int is_alpa(char w);
// interface
void interface();
#endif