#include <iostream>
#include <vector>
using namespace std;

void findPermut(vector<int>& input, int checkSize, int* num){
	if(num>toNum(input)) { cout num; return; }
	
	int checkIndex = input.size()-checkSize;
	
	// checkSize자리 순열 찾기
	vector<int*> permutList;
	int *tmp = new int [input.size()];
	for(int i=0;i<facorial(checkSize());++i){
		for(int i=checkIndex;i<input.size();++i){
			
		}
	}
	
}